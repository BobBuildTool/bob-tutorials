checkoutDeterministic: True
checkoutScript: |
    cp $<<src/lib.c>> lib.c

multiPackage:
    debug:
        buildScript: |
            gcc -c -O0 -g -o lib.o $1/lib.c
        packageScript: |
            cp -a $1/lib.o .
    release:
        buildScript: |
            gcc  -O3 -c -o lib.o $1/lib.c
        packageScript: |
            cp -a $1/lib.o .